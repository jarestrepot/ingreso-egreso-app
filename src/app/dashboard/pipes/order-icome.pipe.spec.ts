import { OrderIcomePipe } from './order-icome.pipe';

describe('OrderIcomePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderIcomePipe();
    expect(pipe).toBeTruthy();
  });
});
